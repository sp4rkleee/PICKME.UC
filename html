<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PICKME UC SHOP</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-purple-500">
    <div class="container mx-auto p-4">
        <h1 class="text-2xl text-white text-center">PICKME UC SHOP</h1>
        <p class="text-white text-center">Вы покупаете код пополнения, который надо будет активировать на сайте.</p>
        <div class="grid grid-cols-3 gap-4 mt-4">
            <!-- Товары -->
            <div class="bg-gray-800 rounded p-4" v-for="product in products">
                <h2 class="text-xl text-white">{{ product.name }}</h2>
                <p class="text-white">Цена: {{ product.price }} ₽</p>
                <button class="bg-blue-500 text-white px-4 py-2 rounded" @click="addToCart(product.id)">Добавить в корзину</button>
            </div>
        </div>
        <!-- Корзина -->
        <div class="mt-8">
            <h2 class="text-xl text-white text-center">Корзина</h2>
            <ul class="list-reset">
                <li v-for="item in cart" class="bg-gray-800 p-4 rounded mb-2">
                    {{ item.name }} - {{ item.price }} ₽
                </li>
            </ul>
            <button class="bg-green-500 text-white px-4 py-2 rounded mt-4 block mx-auto">Оформить заказ</button>
        </div>
    </div>
</body>
</html>
